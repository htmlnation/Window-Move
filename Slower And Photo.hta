 <html>
<head>
<title>My HTML application</title>
<HTA:APPLICATION
  APPLICATIONNAME="ation"
  ID="MyHTMLapplication"
  VERSION="1.0"
  CAPTION="no"
  SYSMENU="no"
  scroll="no"
  showInTaskbar="no"
  MAXIMIZEBUTTON="no"
  MINIMIZEBUTTON="no"/>
 
 
</head>

<script language="VBScript">
window.resizeTo 240,280


Sub Window_OnLoad

  window.moveTo 0, 0
  idTimer = window.setTimeout("a1", 500, "VBScript") 
End Sub

Sub a1
 window.moveTo 100, 0
 idTimer = window.setTimeout("a2", 500, "VBScript")
End Sub

Sub a2
 window.moveTo 200, 0
 idTimer = window.setTimeout("a3", 500, "VBScript")
End Sub

Sub a3
 window.moveTo 300, 0
 idTimer = window.setTimeout("a4", 500, "VBScript")
End Sub

Sub a4
 window.moveTo 400, 0
 idTimer = window.setTimeout("a5", 500, "VBScript")
End Sub

Sub a5
 window.moveTo 500, 0
 idTimer = window.setTimeout("a6", 500, "VBScript")
End Sub

Sub a6
 window.moveTo 600, 0
 idTimer = window.setTimeout("a7", 500, "VBScript")
End Sub

Sub a7
 window.moveTo 700, 0
 idTimer = window.setTimeout("a8", 500, "VBScript")
End Sub

Sub a8
 window.moveTo 800, 0
 idTimer = window.setTimeout("a9", 1000, "VBScript")
End Sub

Sub a9
 window.moveTo 900, 0
 idTimer = window.setTimeout("a10", 500, "VBScript")
End Sub

Sub a10
 window.moveTo 900, 100
 idTimer = window.setTimeout("a11", 500, "VBScript")
End Sub

Sub a11
 window.moveTo 800, 100
 idTimer = window.setTimeout("a12", 500, "VBScript")
End Sub

Sub a12
 window.moveTo 700, 100
 idTimer = window.setTimeout("a13", 500, "VBScript")
End Sub

Sub a13
 window.moveTo 600, 100
 idTimer = window.setTimeout("a14", 500, "VBScript")
End Sub

Sub a14
 window.moveTo 500, 100
 idTimer = window.setTimeout("a15", 500, "VBScript")
End Sub

Sub a15
 window.moveTo 400, 100
 me.close
End Sub

</script>

<body background="image.jpg">

<img src="c:\image.jpg">

<!--Add your controls here-->

<!--{{InsertControlsHere}}-Do not remove this line-->
</body>
</html>